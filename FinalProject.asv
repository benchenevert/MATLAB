%% Variables

x = 0;
h = [];
a = 0;
a = x + h
%% Attempt 1
%%function slope = (f(x+h)-f(x))/h

function f = input()
function g = input()

    %for h = ("value")
    %    a = x + h;

     %   while f = ("value")
            

        

b = (g - f) / h;

print(jumble);
end
end

%end